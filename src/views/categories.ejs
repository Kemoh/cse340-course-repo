<%- include("partials/header") %>
<main>
  <div class="category">
    <h1><%= title %></h1>
    <p>Below are all available service project Categories:</p>
    <ul>
        <% categories.forEach(category => { %>
          <li>
            <%= category.name %>
          </li>
      <%  }); %>
    </ul>
  </div>
</main>
<%- include("partials/footer") %>
